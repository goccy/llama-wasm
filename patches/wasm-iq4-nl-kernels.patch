diff --git a/ggml/src/ggml-cpu/quants.c b/ggml/src/ggml-cpu/quants.c
index 5b52daed9..3807d498c 100644
--- a/ggml/src/ggml-cpu/quants.c
+++ b/ggml/src/ggml-cpu/quants.c
@@ -1287,6 +1287,12 @@ void ggml_vec_dot_iq1_m_q8_K_generic(int n, float * GGML_RESTRICT s, size_t bs,
     *s = sumf;
 }
 
+#if defined(__wasm_simd128__)
+// On wasm this generic body IS the kernel (arch-fallback.h names it
+// ggml_vec_dot_iq4_nl_q8_0); exported out of line under a dbg_* name so a
+// wasm-to-native transpiler can substitute a native body.
+__attribute__((noinline, export_name("dbg_vec_dot_iq4_nl_q8_0")))
+#endif
 void ggml_vec_dot_iq4_nl_q8_0_generic(int n, float * GGML_RESTRICT s, size_t bs, const void * GGML_RESTRICT vx, size_t bx, const void * GGML_RESTRICT vy, size_t by, int nrc) {
     assert(nrc == 1);
     UNUSED(nrc);
diff --git a/ggml/src/ggml-cpu/repack.cpp b/ggml/src/ggml-cpu/repack.cpp
index 90db1dd8d..27f3f2a39 100644
--- a/ggml/src/ggml-cpu/repack.cpp
+++ b/ggml/src/ggml-cpu/repack.cpp
@@ -1201,6 +1201,14 @@ void ggml_gemv_iq4_nl_4x4_q8_0_generic(int n, float * GGML_RESTRICT s, size_t bs
     }
 }
 
+#if defined(__wasm_simd128__)
+// On wasm this generic body IS the kernel (arch-fallback.h names it
+// ggml_gem*_iq4_nl_8x8_q8_0); exported out of line under a dbg_* name so a
+// wasm-to-native transpiler can substitute a native body. The export
+// must carry the body itself: a forwarding wrapper gets inlined into
+// the repack driver by the wasm optimizer and the export goes unused.
+__attribute__((noinline, export_name("dbg_gemv_iq4_nl_8x8")))
+#endif
 void ggml_gemv_iq4_nl_8x8_q8_0_generic(int n, float * GGML_RESTRICT s, size_t bs, const void * GGML_RESTRICT vx, const void * GGML_RESTRICT vy, int nr, int nc) {
     const int qk = QK8_0;
     const int nb = n / qk;
@@ -2221,6 +2229,14 @@ void ggml_gemm_iq4_nl_4x4_q8_0_generic(int n, float * GGML_RESTRICT s, size_t bs
     }
 }
 
+#if defined(__wasm_simd128__)
+// On wasm this generic body IS the kernel (arch-fallback.h names it
+// ggml_gem*_iq4_nl_8x8_q8_0); exported out of line under a dbg_* name so a
+// wasm-to-native transpiler can substitute a native body. The export
+// must carry the body itself: a forwarding wrapper gets inlined into
+// the repack driver by the wasm optimizer and the export goes unused.
+__attribute__((noinline, export_name("dbg_gemm_iq4_nl_8x8")))
+#endif
 void ggml_gemm_iq4_nl_8x8_q8_0_generic(int n, float * GGML_RESTRICT s, size_t bs, const void * GGML_RESTRICT vx, const void * GGML_RESTRICT vy, int nr, int nc) {
     const int qk = QK8_0;
     const int nb = n / qk;
@@ -4843,6 +4859,14 @@ static const ggml::cpu::tensor_traits * ggml_repack_get_optimal_repack_type(cons
             }
         }
     } else if (cur->type == GGML_TYPE_IQ4_NL) {
+#if defined(__wasm_simd128__)
+        // The wasm build exports the iq4_nl 8x8 GEMV/GEMM under dbg_* names
+        // for a wasm-to-native transpiler to substitute native bodies, the
+        // way the q4_K 8x8 pair works.
+        if (cur->ne[1] % 8 == 0) {
+            return &iq4_nl_8x8_q8_0;
+        }
+#endif
         if (ggml_cpu_has_avx2()) {
             if (cur->ne[1] % 8 == 0) {
                 return &iq4_nl_8x8_q8_0;
