diff --git a/ggml/src/ggml-cpu/repack.cpp b/ggml/src/ggml-cpu/repack.cpp
index 32287a5..2b809d7 100644
--- a/ggml/src/ggml-cpu/repack.cpp
+++ b/ggml/src/ggml-cpu/repack.cpp
@@ -259,6 +259,12 @@ void ggml_quantize_mat_q8_K_4x4_generic(const float * GGML_RESTRICT x, void * GG
     }
 }
 
+#if defined(__wasm_simd128__)
+// On wasm this generic body IS ggml_quantize_mat_q8_K_4x8 (arch-fallback.h);
+// exported out of line under a dbg_* name so a wasm-to-native transpiler
+// can substitute a native body, the way the repack GEMV/GEMM are.
+__attribute__((noinline, export_name("dbg_quantize_mat_q8_K_4x8")))
+#endif
 void ggml_quantize_mat_q8_K_4x8_generic(const float * GGML_RESTRICT x, void * GGML_RESTRICT vy, int64_t k) {
     assert(QK_K == 256);
     assert(k % QK_K == 0);
