diff --git a/ggml/src/ggml-cpu/repack.cpp b/ggml/src/ggml-cpu/repack.cpp
index c9fafe304..2cabb3b4c 100644
--- a/ggml/src/ggml-cpu/repack.cpp
+++ b/ggml/src/ggml-cpu/repack.cpp
@@ -852,6 +852,14 @@ void ggml_gemv_q4_0_4x8_q8_0_generic(int n, float * GGML_RESTRICT s, size_t bs,
     }
 }
 
+#if defined(__wasm_simd128__)
+// On wasm this generic body IS the kernel (arch-fallback.h names it
+// ggml_gem*_q4_0_8x8_q8_0); exported out of line under a dbg_* name so a
+// wasm-to-native transpiler can substitute a native body. The export
+// must carry the body itself: a forwarding wrapper gets inlined into
+// the repack driver by the wasm optimizer and the export goes unused.
+__attribute__((noinline, export_name("dbg_gemv_q4_0_8x8")))
+#endif
 void ggml_gemv_q4_0_8x8_q8_0_generic(int n, float * GGML_RESTRICT s, size_t bs, const void * GGML_RESTRICT vx, const void * GGML_RESTRICT vy, int nr, int nc) {
     const int qk = QK8_0;
     const int nb = n / qk;
@@ -1793,6 +1801,14 @@ void ggml_gemm_q4_0_4x8_q8_0_generic(int n, float * GGML_RESTRICT s, size_t bs,
     }
 }
 
+#if defined(__wasm_simd128__)
+// On wasm this generic body IS the kernel (arch-fallback.h names it
+// ggml_gem*_q4_0_8x8_q8_0); exported out of line under a dbg_* name so a
+// wasm-to-native transpiler can substitute a native body. The export
+// must carry the body itself: a forwarding wrapper gets inlined into
+// the repack driver by the wasm optimizer and the export goes unused.
+__attribute__((noinline, export_name("dbg_gemm_q4_0_8x8")))
+#endif
 void ggml_gemm_q4_0_8x8_q8_0_generic(int n, float * GGML_RESTRICT s, size_t bs, const void * GGML_RESTRICT vx, const void * GGML_RESTRICT vy, int nr, int nc) {
     const int qk = QK8_0;
     const int nb = n / qk;
@@ -4686,6 +4702,14 @@ static const ggml::cpu::tensor_traits * ggml_repack_get_optimal_repack_type(cons
 #endif
 
     if (cur->type == GGML_TYPE_Q4_0) {
+#if defined(__wasm_simd128__)
+        // The wasm build exports the q4_0 8x8 GEMV/GEMM under dbg_* names
+        // for a wasm-to-native transpiler to substitute native bodies, the
+        // way the q4_K 8x8 pair works.
+        if (cur->ne[1] % 8 == 0) {
+            return &q4_0_8x8_q8_0;
+        }
+#endif
         if (ggml_cpu_has_avx2() || (ggml_cpu_has_sve() && ggml_cpu_has_matmul_int8() && ggml_cpu_get_sve_cnt() == QK8_0)) {
             if (cur->ne[1] % 8 == 0) {
                 return &q4_0_8x8_q8_0;
