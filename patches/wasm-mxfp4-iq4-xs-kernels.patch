diff --git a/ggml/src/ggml-cpu/quants.c b/ggml/src/ggml-cpu/quants.c
index 031c0448f..b531804bf 100644
--- a/ggml/src/ggml-cpu/quants.c
+++ b/ggml/src/ggml-cpu/quants.c
@@ -295,6 +295,12 @@ void ggml_vec_dot_q4_1_q8_1_generic(int n, float * GGML_RESTRICT s, size_t bs, c
     *s = sumf;
 }
 
+#if defined(__wasm_simd128__)
+// On wasm this generic body IS the kernel (arch-fallback.h names it
+// ggml_vec_dot_mxfp4_q8_0); exported out of line under a dbg_* name so a
+// wasm-to-native transpiler can substitute a native body.
+__attribute__((noinline, export_name("dbg_vec_dot_mxfp4_q8_0")))
+#endif
 void ggml_vec_dot_mxfp4_q8_0_generic(int n, float * GGML_RESTRICT s, size_t bs, const void * GGML_RESTRICT vx, size_t bx, const void * GGML_RESTRICT vy, size_t by, int nrc) {
     assert(nrc == 1);
     UNUSED(nrc);
@@ -1286,6 +1292,12 @@ void ggml_vec_dot_iq4_nl_q8_0_generic(int n, float * GGML_RESTRICT s, size_t bs,
     *s = sumf;
 }
 
+#if defined(__wasm_simd128__)
+// On wasm this generic body IS the kernel (arch-fallback.h names it
+// ggml_vec_dot_iq4_xs_q8_K); exported out of line under a dbg_* name so a
+// wasm-to-native transpiler can substitute a native body.
+__attribute__((noinline, export_name("dbg_vec_dot_iq4_xs_q8_K")))
+#endif
 void ggml_vec_dot_iq4_xs_q8_K_generic(int n, float * GGML_RESTRICT s, size_t bs, const void * GGML_RESTRICT vx, size_t bx, const void * GGML_RESTRICT vy, size_t by, int nrc) {
     assert(nrc == 1);
     UNUSED(nrc);
diff --git a/ggml/src/ggml-cpu/repack.cpp b/ggml/src/ggml-cpu/repack.cpp
index be6833d2b..5d526377a 100644
--- a/ggml/src/ggml-cpu/repack.cpp
+++ b/ggml/src/ggml-cpu/repack.cpp
@@ -1241,6 +1241,14 @@ void ggml_gemv_mxfp4_4x4_q8_0_generic(int n, float * GGML_RESTRICT s, size_t bs,
     }
 }
 
+#if defined(__wasm_simd128__)
+// On wasm this generic body IS the kernel (arch-fallback.h names it
+// ggml_gemv_mxfp4_8x8_q8_0); exported out of line under a dbg_* name so a
+// wasm-to-native transpiler can substitute a native body. The export
+// must carry the body itself: a forwarding wrapper gets inlined into
+// the repack driver by the wasm optimizer and the export goes unused.
+__attribute__((noinline, export_name("dbg_gemv_mxfp4_8x8")))
+#endif
 void ggml_gemv_mxfp4_8x8_q8_0_generic(int n, float * GGML_RESTRICT s, size_t bs, const void * GGML_RESTRICT vx, const void * GGML_RESTRICT vy, int nr, int nc) {
     const int qk = QK8_0;
     const int nb = n / qk;
@@ -2249,6 +2257,14 @@ void ggml_gemm_mxfp4_4x4_q8_0_generic(int n, float * GGML_RESTRICT s, size_t bs,
     }
 }
 
+#if defined(__wasm_simd128__)
+// On wasm this generic body IS the kernel (arch-fallback.h names it
+// ggml_gemm_mxfp4_8x8_q8_0); exported out of line under a dbg_* name so a
+// wasm-to-native transpiler can substitute a native body. The export
+// must carry the body itself: a forwarding wrapper gets inlined into
+// the repack driver by the wasm optimizer and the export goes unused.
+__attribute__((noinline, export_name("dbg_gemm_mxfp4_8x8")))
+#endif
 void ggml_gemm_mxfp4_8x8_q8_0_generic(int n, float * GGML_RESTRICT s, size_t bs, const void * GGML_RESTRICT vx, const void * GGML_RESTRICT vy, int nr, int nc) {
     const int qk = QK8_0;
     const int nb = n / qk;
@@ -4715,6 +4731,14 @@ static const ggml::cpu::tensor_traits * ggml_repack_get_optimal_repack_type(cons
                 return &mxfp4_8x8_q8_0;
             }
         }
+#if defined(__wasm_simd128__)
+        // The wasm build exports the mxfp4 8x8 GEMV/GEMM under dbg_* names
+        // for a wasm-to-native transpiler to substitute native bodies, the
+        // way the iq4_nl 8x8 pair works.
+        if (cur->ne[1] % 8 == 0) {
+            return &mxfp4_8x8_q8_0;
+        }
+#endif
         if (ggml_cpu_has_neon() && ggml_cpu_has_dotprod()) {
             if (cur->ne[1] % 4 == 0) {
                 return &mxfp4_4x4_q8_0;
