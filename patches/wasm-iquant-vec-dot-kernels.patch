diff --git a/ggml/src/ggml-cpu/quants.c b/ggml/src/ggml-cpu/quants.c
index 031c0448f..e46205887 100644
--- a/ggml/src/ggml-cpu/quants.c
+++ b/ggml/src/ggml-cpu/quants.c
@@ -124,6 +124,12 @@ void quantize_row_q8_K_generic(const float * GGML_RESTRICT x, void * GGML_RESTRI
 
 //===================================== Dot products =================================
 
+#if defined(__wasm_simd128__)
+// On wasm this generic body IS the kernel (arch-fallback.h names it
+// ggml_vec_dot_q1_0_q8_0); exported out of line under a dbg_* name so a
+// wasm-to-native transpiler can substitute a native body.
+__attribute__((noinline, export_name("dbg_vec_dot_q1_0_q8_0")))
+#endif
 void ggml_vec_dot_q1_0_q8_0_generic(int n, float * GGML_RESTRICT s, size_t bs, const void * GGML_RESTRICT vx, size_t bx, const void * GGML_RESTRICT vy, size_t by, int nrc) {
     const int qk = QK1_0;
     const int nb = n / qk;
@@ -174,6 +180,12 @@ void ggml_vec_dot_q1_0_q8_0_generic(int n, float * GGML_RESTRICT s, size_t bs, c
     *s = sumf;
 }
 
+#if defined(__wasm_simd128__)
+// On wasm this generic body IS the kernel (arch-fallback.h names it
+// ggml_vec_dot_q2_0_q8_0); exported out of line under a dbg_* name so a
+// wasm-to-native transpiler can substitute a native body.
+__attribute__((noinline, export_name("dbg_vec_dot_q2_0_q8_0")))
+#endif
 void ggml_vec_dot_q2_0_q8_0_generic(int n, float * GGML_RESTRICT s, size_t bs, const void * GGML_RESTRICT vx, size_t bx, const void * GGML_RESTRICT vy, size_t by, int nrc) {
     const int qk = QK2_0;
     const int nb = n / qk;
@@ -327,6 +339,12 @@ void ggml_vec_dot_mxfp4_q8_0_generic(int n, float * GGML_RESTRICT s, size_t bs,
 }
 
 // NVFP4: super-block of 64 elements = 4 sub-blocks of 16 = 2 q8_0 blocks
+#if defined(__wasm_simd128__)
+// On wasm this generic body IS the kernel (arch-fallback.h names it
+// ggml_vec_dot_nvfp4_q8_0); exported out of line under a dbg_* name so a
+// wasm-to-native transpiler can substitute a native body.
+__attribute__((noinline, export_name("dbg_vec_dot_nvfp4_q8_0")))
+#endif
 void ggml_vec_dot_nvfp4_q8_0_generic(int n, float * GGML_RESTRICT s, size_t bs, const void * GGML_RESTRICT vx, size_t bx, const void * GGML_RESTRICT vy, size_t by, int nrc) {
     assert(nrc == 1);
     UNUSED(nrc);
@@ -478,6 +496,12 @@ void ggml_vec_dot_q8_0_q8_0_generic(int n, float * GGML_RESTRICT s, size_t bs, c
     *s = sumf;
 }
 
+#if defined(__wasm_simd128__)
+// On wasm this generic body IS the kernel (arch-fallback.h names it
+// ggml_vec_dot_tq1_0_q8_K); exported out of line under a dbg_* name so a
+// wasm-to-native transpiler can substitute a native body.
+__attribute__((noinline, export_name("dbg_vec_dot_tq1_0_q8_K")))
+#endif
 void ggml_vec_dot_tq1_0_q8_K_generic(int n, float * GGML_RESTRICT s, size_t bs, const void * GGML_RESTRICT vx, size_t bx, const void * GGML_RESTRICT vy, size_t by, int nrc) {
     assert(nrc == 1);
     UNUSED(nrc);
@@ -530,6 +554,12 @@ void ggml_vec_dot_tq1_0_q8_K_generic(int n, float * GGML_RESTRICT s, size_t bs,
     *s = sumf;
 }
 
+#if defined(__wasm_simd128__)
+// On wasm this generic body IS the kernel (arch-fallback.h names it
+// ggml_vec_dot_tq2_0_q8_K); exported out of line under a dbg_* name so a
+// wasm-to-native transpiler can substitute a native body.
+__attribute__((noinline, export_name("dbg_vec_dot_tq2_0_q8_K")))
+#endif
 void ggml_vec_dot_tq2_0_q8_K_generic(int n, float * GGML_RESTRICT s, size_t bs, const void * GGML_RESTRICT vx, size_t bx, const void * GGML_RESTRICT vy, size_t by, int nrc) {
     assert(nrc == 1);
     UNUSED(nrc);
@@ -903,6 +933,12 @@ void ggml_vec_dot_q6_K_q8_K_generic(int n, float * GGML_RESTRICT s, size_t bs, c
     *s = sumf;
 }
 
+#if defined(__wasm_simd128__)
+// On wasm this generic body IS the kernel (arch-fallback.h names it
+// ggml_vec_dot_iq2_xxs_q8_K); exported out of line under a dbg_* name so a
+// wasm-to-native transpiler can substitute a native body.
+__attribute__((noinline, export_name("dbg_vec_dot_iq2_xxs_q8_K")))
+#endif
 void ggml_vec_dot_iq2_xxs_q8_K_generic(int n, float * GGML_RESTRICT s, size_t bs, const void * GGML_RESTRICT vx, size_t bx, const void * GGML_RESTRICT vy, size_t by, int nrc) {
     assert(n % QK_K == 0);
     assert(nrc == 1);
@@ -945,6 +981,12 @@ void ggml_vec_dot_iq2_xxs_q8_K_generic(int n, float * GGML_RESTRICT s, size_t bs
     *s = 0.125f * sumf;
 }
 
+#if defined(__wasm_simd128__)
+// On wasm this generic body IS the kernel (arch-fallback.h names it
+// ggml_vec_dot_iq2_xs_q8_K); exported out of line under a dbg_* name so a
+// wasm-to-native transpiler can substitute a native body.
+__attribute__((noinline, export_name("dbg_vec_dot_iq2_xs_q8_K")))
+#endif
 void ggml_vec_dot_iq2_xs_q8_K_generic(int n, float * GGML_RESTRICT s, size_t bs, const void * GGML_RESTRICT vx, size_t bx, const void * GGML_RESTRICT vy, size_t by, int nrc) {
     assert(n % QK_K == 0);
     assert(nrc == 1);
@@ -995,6 +1037,12 @@ void ggml_vec_dot_iq2_xs_q8_K_generic(int n, float * GGML_RESTRICT s, size_t bs,
     *s = 0.125f * sumf;
 }
 
+#if defined(__wasm_simd128__)
+// On wasm this generic body IS the kernel (arch-fallback.h names it
+// ggml_vec_dot_iq2_s_q8_K); exported out of line under a dbg_* name so a
+// wasm-to-native transpiler can substitute a native body.
+__attribute__((noinline, export_name("dbg_vec_dot_iq2_s_q8_K")))
+#endif
 void ggml_vec_dot_iq2_s_q8_K_generic(int n, float * GGML_RESTRICT s, size_t bs, const void * GGML_RESTRICT vx, size_t bx, const void * GGML_RESTRICT vy, size_t by, int nrc) {
     assert(n % QK_K == 0);
     assert(nrc == 1);
@@ -1047,6 +1095,12 @@ void ggml_vec_dot_iq2_s_q8_K_generic(int n, float * GGML_RESTRICT s, size_t bs,
     *s = 0.125f * sumf;
 }
 
+#if defined(__wasm_simd128__)
+// On wasm this generic body IS the kernel (arch-fallback.h names it
+// ggml_vec_dot_iq3_xxs_q8_K); exported out of line under a dbg_* name so a
+// wasm-to-native transpiler can substitute a native body.
+__attribute__((noinline, export_name("dbg_vec_dot_iq3_xxs_q8_K")))
+#endif
 void ggml_vec_dot_iq3_xxs_q8_K_generic(int n, float * GGML_RESTRICT s, size_t bs, const void * GGML_RESTRICT vx, size_t bx, const void * GGML_RESTRICT vy, size_t by, int nrc) {
     assert(n % QK_K == 0);
     assert(nrc == 1);
@@ -1091,6 +1145,12 @@ void ggml_vec_dot_iq3_xxs_q8_K_generic(int n, float * GGML_RESTRICT s, size_t bs
     *s = 0.25f * sumf;
 }
 
+#if defined(__wasm_simd128__)
+// On wasm this generic body IS the kernel (arch-fallback.h names it
+// ggml_vec_dot_iq3_s_q8_K); exported out of line under a dbg_* name so a
+// wasm-to-native transpiler can substitute a native body.
+__attribute__((noinline, export_name("dbg_vec_dot_iq3_s_q8_K")))
+#endif
 void ggml_vec_dot_iq3_s_q8_K_generic(int n, float * GGML_RESTRICT s, size_t bs, const void * GGML_RESTRICT vx, size_t bx, const void * GGML_RESTRICT vy, size_t by, int nrc) {
     assert(n % QK_K == 0);
     assert(nrc == 1);
@@ -1147,6 +1207,12 @@ void ggml_vec_dot_iq3_s_q8_K_generic(int n, float * GGML_RESTRICT s, size_t bs,
     *s = sumf;
 }
 
+#if defined(__wasm_simd128__)
+// On wasm this generic body IS the kernel (arch-fallback.h names it
+// ggml_vec_dot_iq1_s_q8_K); exported out of line under a dbg_* name so a
+// wasm-to-native transpiler can substitute a native body.
+__attribute__((noinline, export_name("dbg_vec_dot_iq1_s_q8_K")))
+#endif
 void ggml_vec_dot_iq1_s_q8_K_generic(int n, float * GGML_RESTRICT s, size_t bs, const void * GGML_RESTRICT vx, size_t bx, const void * GGML_RESTRICT vy, size_t by, int nrc) {
     assert(n % QK_K == 0);
     assert(nrc == 1);
@@ -1190,6 +1256,12 @@ void ggml_vec_dot_iq1_s_q8_K_generic(int n, float * GGML_RESTRICT s, size_t bs,
     *s = sumf;
 }
 
+#if defined(__wasm_simd128__)
+// On wasm this generic body IS the kernel (arch-fallback.h names it
+// ggml_vec_dot_iq1_m_q8_K); exported out of line under a dbg_* name so a
+// wasm-to-native transpiler can substitute a native body.
+__attribute__((noinline, export_name("dbg_vec_dot_iq1_m_q8_K")))
+#endif
 void ggml_vec_dot_iq1_m_q8_K_generic(int n, float * GGML_RESTRICT s, size_t bs, const void * GGML_RESTRICT vx, size_t bx, const void * GGML_RESTRICT vy, size_t by, int nrc) {
     assert(n % QK_K == 0);
     assert(nrc == 1);
